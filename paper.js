class paper {
    constructor(){
        var options = {
            isStatic : false,
            restitution : 0.3,
            friction : 0,
            density : 1.2
         }
        display()
        {
            this.image = loadImage("images/paper.png");

        }
    }
}image = loadImage("images/paper.png")